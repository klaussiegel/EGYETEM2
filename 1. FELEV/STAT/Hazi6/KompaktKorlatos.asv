function X = KompaktKorlatos(n)
    f = @(x,y)(fuggveny(x,y));
    
%     Minden i = 1; 2; : : : ; n indexre vegezd el:
%     2 ismeteld:
%     3 generald a fuggetlen U  U ([0; 1]) es V  U ([0; 1]) valoszn}usegi
%     valtozokat
%     4 Y   a + (b ? a)V
%     5 ameddig UM  f (Y)
%     6 Xi = Y.

    for i=
    
    
    hist(X);
end